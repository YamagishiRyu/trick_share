%h1
  記事一覧

%br

= form_tag({:controller=>'tricks', :action=>'search'}) do |f|
  = text_field_tag 'search_word',"", :class=>'col-md-4', :style=>"margin-right: 10px; margin-top: 4px;"
  = submit_tag '検索', :class => 'btn btn-primary'

%hr
%ol.list-unstyled#trick-list
  = render partial: "trick_easy", collection: @tricks, as: :trick 
= paginate @tricks
